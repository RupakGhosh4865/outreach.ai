FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production

# Install dependencies, omitting devDependencies for a smaller image
COPY package*.json ./
RUN npm install --omit=dev

# Copy application code
COPY . .

EXPOSE 5000
CMD ["npm", "start"]
